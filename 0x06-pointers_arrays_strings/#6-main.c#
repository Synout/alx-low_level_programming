#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int main(void)
{
char str0[] = "Expect the best. Prepare for the worst. Capitalize on what comes.";
char str1[] = "\nhello world! hello-world 0123456hello world\thello world.hello world\n";
char str[] = str0 + str1;r;

ptr = cap_string(str);
printf("%s", ptr);
printf("%s", str);
return (0);
}
