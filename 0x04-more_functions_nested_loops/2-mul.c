#include "main.h"

/**
 * mul - function that multiplies two int numbers
 * @a : first number to multiply
 * @b : second number to multiply
 * Return: int
 */

int mul(int a, int b)
{
return (a * b);
}
