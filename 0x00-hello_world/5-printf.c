#include <stdio.h>

/**
 * main - entry point function
 * Description -  prints a predefined text using printf
 * Return: Always 0 (no errors)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
