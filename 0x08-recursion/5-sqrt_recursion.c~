#include "main.h"

/**
 * _pow_recursion - counts len of chars using recursion
 * @x : integer
 * @y : power
 * Return: int
 */


int _pow_recursion(int x, int y)
{

if (y == 0)
{

return (1);

}
if (y < 0)
{
return (-1);
}
else
{
x *= _pow_recursion(x, y - 1);
}
return (x);

}
