#include "main.h"

/**
 * _sqrt_recursion - finds sqrt of int using recursion
 * @x : integer
 * Return: int
 */


int _sqrt_recursion(int n)
{
int i = 0;
if (n == 0)
{

return (0);

}
if (y < 0)
{
return (-1);
}
else if (i * i == n)
{
return (i);
}
else
{
i +=  _sqrt_recursion(++i);
}
return (x);

}
